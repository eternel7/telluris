---
type: llm
focus: last_message
weight: 1
---
In the components list the CATALYST appears BEFORE the CONSUMED component, and the answer explains why: the life-link value is overwritten by the last engaged component rather than added up, so putting the consumed one last keeps the stronger link when both are used. An answer that simply adds the two component bonuses together, or that orders them without this explanation, fails.
